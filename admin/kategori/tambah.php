<?php
require_once __DIR__.'/../../config/auth.php';require_once __DIR__.'/../../config/database.php';require_once __DIR__.'/../../config/helper.php';require_admin();$pdo=db();$errors=[];$nama='';
if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$nama=trim((string)($_POST['nama']??''));if(!$pdo)$errors[]='Database belum terhubung.';if(mb_strlen($nama)<3)$errors[]='Nama kategori minimal 3 karakter.';if(!$errors&&$pdo){$slug=slugify($nama);$c=$pdo->prepare('SELECT id FROM kategori_materi WHERE slug=?');$c->execute([$slug]);if($c->fetch())$errors[]='Kategori dengan slug yang sama sudah ada.';else{$s=$pdo->prepare('INSERT INTO kategori_materi(nama,slug) VALUES(?,?)');$s->execute([$nama,$slug]);flash('success','Kategori berhasil ditambahkan.');redirect('admin/kategori/index.php');}}}
$adminPageTitle='Tambah Kategori';$adminPageDescription='Buat kelompok baru untuk materi belajar.';$adminActive='kategori';include __DIR__.'/../../includes/admin_header.php';
?>
<section class="admin-card admin-form-card"><div class="admin-card-head"><div><h2>Kategori Baru</h2><p>Slug dibuat otomatis dari nama kategori.</p></div></div><?php if($errors):?><div class="notice"><ul class="error-list"><?php foreach($errors as $e):?><li><?=e($e)?></li><?php endforeach;?></ul></div><?php endif;?><form class="admin-form" method="post"><?=csrf_field()?><label>Nama Kategori</label><input name="nama" value="<?=e($nama)?>" required><div class="admin-form-actions"><button class="btn">Simpan Kategori</button><a class="btn ghost" href="<?=url('admin/kategori/index.php')?>">Batal</a></div></form></section><?php include __DIR__.'/../../includes/admin_footer.php'; ?>
