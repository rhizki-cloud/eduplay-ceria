<?php
require_once __DIR__.'/../../config/auth.php';require_once __DIR__.'/../../config/database.php';require_once __DIR__.'/../../config/helper.php';require_admin();$pdo=db();$id=(int)($_GET['id']??$_POST['id']??0);if(!$pdo||!$id){flash('error','Kategori tidak ditemukan.');redirect('admin/kategori/index.php');}$s=$pdo->prepare('SELECT * FROM kategori_materi WHERE id=?');$s->execute([$id]);$row=$s->fetch();if(!$row){flash('error','Kategori tidak ditemukan.');redirect('admin/kategori/index.php');}$errors=[];
if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$nama=trim((string)($_POST['nama']??''));if(mb_strlen($nama)<3)$errors[]='Nama kategori minimal 3 karakter.';$slug=slugify($nama);$c=$pdo->prepare('SELECT id FROM kategori_materi WHERE slug=? AND id<>?');$c->execute([$slug,$id]);if($c->fetch())$errors[]='Slug kategori sudah digunakan.';if(!$errors){$u=$pdo->prepare('UPDATE kategori_materi SET nama=?,slug=? WHERE id=?');$u->execute([$nama,$slug,$id]);flash('success','Kategori berhasil diperbarui.');redirect('admin/kategori/index.php');}$row['nama']=$nama;}
$adminPageTitle='Edit Kategori';$adminPageDescription='Perbarui nama kelompok materi.';$adminActive='kategori';include __DIR__.'/../../includes/admin_header.php';
?>
<section class="admin-card admin-form-card"><div class="admin-card-head"><div><h2><?=e($row['nama'])?></h2><p>Slug akan diperbarui otomatis.</p></div></div><?php if($errors):?><div class="notice"><ul class="error-list"><?php foreach($errors as $e):?><li><?=e($e)?></li><?php endforeach;?></ul></div><?php endif;?><form class="admin-form" method="post"><?=csrf_field()?><input type="hidden" name="id" value="<?=$id?>"><label>Nama Kategori</label><input name="nama" value="<?=e($row['nama'])?>" required><div class="admin-form-actions"><button class="btn">Simpan Perubahan</button><a class="btn ghost" href="<?=url('admin/kategori/index.php')?>">Batal</a></div></form></section><?php include __DIR__.'/../../includes/admin_footer.php'; ?>
