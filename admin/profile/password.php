<?php
require_once __DIR__.'/../../config/auth.php';require_once __DIR__.'/../../config/database.php';require_once __DIR__.'/../../config/helper.php';require_admin();$pdo=db();$user=current_user();$errors=[];
if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$old=(string)($_POST['current_password']??'');$new=(string)($_POST['password']??'');$confirm=(string)($_POST['password_confirmation']??'');if(!$pdo)$errors[]='Database belum terhubung.';if(strlen($new)<8)$errors[]='Password baru minimal 8 karakter.';if($new!==$confirm)$errors[]='Konfirmasi password baru tidak sama.';if(!$errors&&$pdo){$s=$pdo->prepare('SELECT password FROM users WHERE id=?');$s->execute([(int)$user['id']]);$hash=$s->fetchColumn();if(!$hash||!password_verify($old,$hash))$errors[]='Password saat ini salah.';else{$u=$pdo->prepare('UPDATE users SET password=? WHERE id=?');$u->execute([password_hash($new,PASSWORD_DEFAULT),(int)$user['id']]);log_activity($pdo,(int)$user['id'],'Mengganti password admin');flash('success','Password berhasil diganti.');redirect('admin/profile/index.php');}}}
$adminPageTitle='Ganti Password';$adminPageDescription='Perbarui password akun administrator.';$adminActive='';include __DIR__.'/../../includes/admin_header.php';
?>
<section class="admin-card admin-form-card"><div class="admin-card-head"><div><h2>Password Administrator</h2><p>Masukkan password saat ini sebagai verifikasi.</p></div></div><?php if($errors):?><div class="notice"><ul class="error-list"><?php foreach($errors as $e):?><li><?=e($e)?></li><?php endforeach;?></ul></div><?php endif;?><form class="admin-form" method="post"><?=csrf_field()?><label>Password Saat Ini</label><input type="password" name="current_password" required autocomplete="current-password"><label>Password Baru</label><input type="password" name="password" required minlength="8" autocomplete="new-password"><label>Ulangi Password Baru</label><input type="password" name="password_confirmation" required minlength="8" autocomplete="new-password"><div class="admin-form-actions"><button class="btn">Ganti Password</button><a class="btn ghost" href="<?=url('admin/profile/index.php')?>">Batal</a></div></form></section>
<?php include __DIR__.'/../../includes/admin_footer.php'; ?>
